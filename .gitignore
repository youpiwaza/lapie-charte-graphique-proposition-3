# Exclude generated files & folders
# dist/ # #Add back /dist as /prod is down, and to give access to people without consoles
prod/

# Exclude npm/yarn package folders
node_modules/

# Exclude package manager files files
package-lock.json
yarn.lock
yarn-error.log

# Exclude Unit tests reports
coverage/
